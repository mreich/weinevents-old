<!DOCTYPE html>
<html>
<head>
  <title>Weinevents | Weinproben, Weinfest und andere Weinveranstaltungen in Ihrer Stadt</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>


<div id="header">
	<h1><%= link_to "Home", root_path %></h1>
	<div id="user_bar">
		<% if logged_in? %>
			<%= link_to "Neues Event", new_event_path %> | 
			<%= link_to "Passwort ändern", edit_user_path(current_user) %> |
			<%= link_to "Abmelden", logout_path %>
		<% else %>
			<%= link_to "Einloggen", login_path %>
			<%= link_to "Anmelden", new_user_path %>
		<% end %>
	</div>
</div>

<div id="main">
	<%= content_tag :p, notice, :class => 'notice' if notice.present? %>
	<%= content_tag :p, alert, :class => 'alert' if alert.present? %>
	<%= yield %>
</div>

<div id="footer">
</div>

</body>
</html>
